// Keep empty in repo to satisfy push protection.
// Set your public token in deployment or local override file when needed.
window.MAPBOX_TOKEN = "";
window.GA_MEASUREMENT_ID = "";
window.LEADS_WEBHOOK_URL = "";
